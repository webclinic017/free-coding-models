# Changelog v0.1.61 - 2026-02-23

## What's new

### TUI Footer & UX

- **"Made with" line is now pink**: the entire "Made with 💖 & ☕ by vava-nessa" sentence is now rendered in soft pink, making it visually distinct from the rest of the footer
- **`K Help` badge is now ultra-visible**: changed from plain green background to bright green (`bgGreenBright`) with **yellow bold text** — stands out immediately at a glance in the footer hint line
- **`P` key closes Settings**: pressing `P` again while inside the Settings screen now closes it (same behavior as `Esc`). Both keys now trigger the same close + provider rebuild logic
