---
id: t6
title: Verify audit-plan implementation and release notes
status: Done
report: |
  ## Changes
  - Verified the audit plan item by item and fixed the remaining gaps.
  - Added OpenRouter HY3, SambaNova DeepSeek-V3.2, and a general Mistral provider.
  - Updated README and CHANGELOG for 0.3.56 with 180 models across 16 providers.
  - Updated provider metadata, env vars, probes, quota capabilities, colors, sync-set filtering, and tests.
  ## Verification
  - `pnpm test` passed: 356 tests, 64 suites, 0 failures.
  - `node --test test/model-merger.test.js` passed: 6 tests, 1 suite, 0 failures.
  - `pnpm start -- --help` exited cleanly.
  - `pnpm start` launched the TUI and exited cleanly.
order: 0
priority: P1
tags: [audit, providers, changelog, verification]
assignee: codex
created: 2026-05-04
ownerType: agent
---

# Verify audit-plan implementation and release notes

## Context

The user asked whether every item from the 2026-05-04 provider audit plan was actually implemented. Verify the current catalog against the audit bullets, patch any missing items, and make sure `CHANGELOG.md` stays on the existing release number while accurately summarizing all major changes since the last release.

## Subtasks

- [x] Compare active catalog against each audit-plan provider/model bullet
  report: Found three real gaps versus the audit plan: OpenRouter was missing `tencent/hy3-preview:free`, SambaNova was missing live `DeepSeek-V3.2`, and Mistral had not been added as a general provider beyond the Codestral shortcut.
- [x] Patch any missing model/provider/config/doc changes
  report: Added OpenRouter HY3, SambaNova DeepSeek-V3.2, a new 7-model Mistral provider, provider metadata/env vars/probes/theme/quota support, and OpenRouter free detection for zero-priced non-`:free` IDs.
- [x] Rebuild `CHANGELOG.md` for the current package version from commits and current diff
  report: Rewrote `CHANGELOG.md` for existing version `0.3.56`, preserving the release number and covering commits since `v0.3.55` plus the major provider catalog update.
- [x] Run tests and runtime verification after corrections
  report: `pnpm test` passed with 356 tests / 64 suites / 0 failures; `node --test test/model-merger.test.js` passed with 6 tests / 1 suite / 0 failures; `pnpm start -- --help` and an interactive `pnpm start` TUI smoke test both exited cleanly.

## Notes

- Keep `package.json` version unchanged.
- Do not commit, push, publish, or bump without explicit approval.
